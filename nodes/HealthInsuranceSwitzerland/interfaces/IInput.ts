export default interface IInput {
  ageCode: string,
  coverCode: string,
  postalCode: string,
  franchise: string,
  locale: string,
  language: string,
  isDollarVersion: boolean;
  spreadsheetId: string,
  sheetPostal: string,
  sheetOFSP: string,
}
